      <div class="row">
        뱅가드랩 홈페이지의 Reference와 관련된 사항을 관리하는 화면입니다.
      </div>
<?php
/*
  // Encrypt / Decrypt
  $strOrg = '0000';
  $strEnc = $this->common_library->encrypt($strOrg);
  $strDec = $this->common_library->decrypt($strEnc);

  echo 'Original : ' . $strOrg . '<br />';
  echo 'Encrypt : ' . $strEnc . '<br />';
  echo 'Decrypt : ' . $strDec . '<br />';
  

  // Current DateTime
  $currTime = new DateTime();
  echo 'Current DateTime is ' . $currTime->format('Y-m-d H:i:s');
  //echo 'Current DateTime is ' . $currTime->y . '-' . $currTime->m . '-' . $currTime->d . ' ' . $currTime->h . ':' . $currTime->i . ':' . $currTime->s . '<br />';


  // Display database.php db lists
  /*$this->common_library->getDbList();
  foreach ($dbList as $key => $value) {
    echo $key . '<br />';
  }*/

  // Display time interval
  /*$startTime = new DateTime();
  print_r($startTime);
  echo '<br />';
  
  sleep(2);
  
  $endTime = new DateTime();
  print_r($endTime);
  echo '<br />';
  
  $diffTime = $startTime->diff($endTime);
  print_r($diffTime);
  echo '<br />';*/

?>